{
  "generatedDir": "${appRoot}/tmp",
  "assets": {
    "defaultNamespace": "default",
    "viewsDir": "${generatedDir}/views/",
    "assetsDir": "${generatedDir}/assets/",
    "publicUrlRoot": "public"
  },
  "containers": {
    "default": {
      "particles": ["${appRoot}/test/root"]
    }
  }
}